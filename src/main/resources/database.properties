# Database Configuration for Hospital Design Patterns Project

# PostgreSQL Database Connection
db.url=jdbc:postgresql://localhost:5432/hospital_db
db.username=taagnes
db.password=663110ak
db.driver=org.postgresql.Driver

# Connection Pool Settings
db.pool.maximum=10
db.pool.minimum=2
db.pool.timeout=30000
db.pool.idle-timeout=600000

# Test Database (H2 in-memory for testing)
test.db.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
test.db.username=sa
test.db.password=
test.db.driver=org.h2.Driver
